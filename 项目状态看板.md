# AI 家庭教师 - 项目状态看板

更新时间: 2026-05-31

目标: 从当前原型逐步推进到可内测、可上架、可运营的成熟 App。

## 当前结论

当前项目处于 **阶段 4 App 化早期**。

已经不只是静态原型: 现在有后端 API、H5/PWA 内测壳、运营控制台和 Expo mobile 工程骨架。但还不是成熟 App, 因为真机、真实服务商、数据库 smoke、支付/IAP 和上架材料还没有完成。

## 已完成

### 后端能力

- Fastify + Prisma + PostgreSQL 架构。
- OTP 登录、session token、账号级数据隔离。
- 单账号只对应一个学生档案, 不做多学生切换。
- 提问、AI 启发式引导、错题入库、D1/D3/D7/D15 复习任务。
- 知识图谱、错题本、家长周报。
- 图片上传、OCR 抽象、内容安全拦截。
- LLM Provider 已支持 OpenAI 兼容接口, 并新增真实 provider 自检命令 `npm run ai:check`。
- 真实模型请求已加入 `LLM_TIMEOUT_MS` 超时控制和 `LLM_MAX_OUTPUT_TOKENS` 输出长度上限。
- Plus 订阅后端雏形: 套餐、订单、checkout、webhook、取消续订、订阅配额。
- 推送后端雏形: device token、复习提醒任务、通知投递记录。
- 管理后台 API: 用户、题目、AI 日志、反馈、内容审核、指标、运营健康、账单对账。
- 数据清理脚本: 上传图片、AI 日志、session、验证码、通知投递、停用设备 token。

### H5 / PWA

- 静态原型页面完整覆盖首页、提问、错题、复习、报告、我的、协议页。
- PWA manifest、service worker、离线页、移动端安装元信息。
- 静态运营控制台 `prototype/admin.html`, 可配置 API 和 `ADMIN_TOKEN` 查看核心运营数据。

### Mobile App

- `mobile/` Expo SDK 56 工程骨架。
- OTP 登录、bearer session API 客户端、SecureStore 保存 API 地址和 session token。
- 首页概览、文字/图片提问、多轮引导消息、加入错题/独立解决。
- 今日复习、错题本、知识图谱、家长周报、Plus 订阅、我的页。
- `expo-image-picker` 拍照/相册识题入口。
- `expo-notifications` 获取 Expo push token 并注册到 `/devices`。
- `mobile:check` 和 `verify:static` 已覆盖 mobile 静态检查。
- 登录页已提供“体验演示”入口, 不配置后端也能查看首页、提问引导、加入错题、复习任务、家长周报和知识图谱。
- 体验演示已补充首页行动卡片、数学/英语/物理示例题和提问流程进度, 打开 App 后能更直观看到核心学习闭环。
- 复习页已补充“答对 / 仍不会”操作; 演示模式可即时减少任务或重置 D1 复习, 真实后端模式调用 `/review-tasks/:id/answer`。
- 移动端复习提交已对齐后端 `answer` 契约, 答对使用变式题正确答案, 仍不会触发后端复习周期重置。
- 错题本已补充状态标签和“去复习这类题”动作, 能从错题直接回到复习任务。
- 家长周报已按“本周结论 / 主要薄弱点 / 家长行动清单”分区展示, 并能跳转到错题或复习。
- 知识图谱已改为状态卡片, 可从薄弱知识点跳到相关错题或复习计划。
- Plus 页已补充免费版/Plus 权益对比和演示开通状态; 我的页已补充账号状态、复习提醒和周报/Plus 入口。
- 我的页已补充内测诊断, 可刷新账号/学生档案/今日额度, 并检查后端 `/health` 和 `/ready`。
- 登录成功或恢复 session 后, mobile 会自动同步账号、首页、复习、错题、周报、知识图谱、套餐和后端 readiness; 我的页保留手动同步入口。
- 登录页已补充 iOS/Android 模拟器 API 快捷配置; `npm run api:local` 可输出真机 Expo Go 使用的局域网后端地址。
- App 身份已从示例包名切换为 `com.zgc1207.aitutor`, 并在静态检查中阻止 `com.example.*` 包名和缺失关键权限说明。
- OTP 登录前已补充显式协议同意动作, 客户端不再默认代填 `consentAccepted`; 同时修正移动端登录学段为后端枚举 `junior`。
- 我的页已补充内测反馈入口, 可提交问题、AI 质量、体验建议和内容安全反馈到 `/feedback`。
- 我的页已补充账号数据导出摘要和二次确认注销入口, 复用后端 `/account/export` 与 `DELETE /account`。
- 登录页和我的页已补充内测版用户协议、隐私政策和未成年人使用说明查看入口。
- 已补充 `mobile/eas.json`、iOS `buildNumber`、Android `versionCode` 和 preview 内测包构建脚本, 静态检查覆盖 EAS profile。
- 已复用原型品牌图标生成移动端图标、Android adaptive icon 和启动屏图标, 并接入 Expo 配置与静态检查。

### 文档与合规

- `上线推进计划.md`
- `技术演进路线.md`
- `阶段2技术设计.md`
- `内测准备清单.md`
- `部署前检查清单.md`
- `合规上架检查清单.md`
- `用户协议草案.md`
- `隐私政策草案.md`
- `未成年人使用说明.md`
- `AI评测记录模板.md`
- `用户验证模板.md`

## 最近验证结果

已通过:

```bash
cd mobile && npm run verify:static
cd server && npm run verify:static
```

Mobile 依赖状态:

- `mobile/package-lock.json` 已生成, 用于锁定 Expo / React Native 依赖版本。
- `mobile/node_modules` 已安装, 当前约 337MB, 属于 Expo / React Native 本地依赖目录。
- `node_modules/` 已在 `.gitignore` 中, 不应提交到版本库。
- 已新增 `npm run start:local`, 可用项目内 `mobile/.expo-home` 缓存启动 Expo, 避免本地环境无法写入 `~/.expo`。
- 2026-05-30 已验证 `cd mobile && npm run start:local` 能启动 Metro, 地址 `exp://127.0.0.1:8081`。
- 2026-05-31 再次执行 `cd mobile && npm run start:local`, Expo 进入 Metro 启动流程, 但当前网络卡在 React Native DevTools 下载; 已新增 `npm run start:local:offline` 和 `npm run start:check`, 用于网络受限时跳过额外下载并确认 Metro 是否真实监听。
- 2026-05-31 执行 `cd mobile && npm run start:check`, 30 秒内未探测到 `127.0.0.1:8082` 监听, 输出停在 `Networking has been disabled` / `Starting project...`; 移动端真机验收仍需解决本机 Expo 启动链路。
- 2026-05-31 已定位 Expo SDK 56 的 `--offline` 会强制 LAN host, 不适合作为 `127.0.0.1` 本机监听验收; `start:local:offline` 改为本地 HOME + headless + 关闭依赖校验, 并为 `start:check` 增加 Expo startup log 诊断和子进程信号转发。
- 2026-05-31 执行 `cd server && npm run runtime:check`, 结果未通过: 当前机器缺少 Docker, `psql` 缺失为 warning; 数据库/API 烟测需安装 Docker Desktop 后运行 `docker compose up -d postgres` 和 `cd server && npm run verify:db`。

`server` 静态验证包含:

- 后端 JS 语法检查
- Prisma Client 生成
- Prisma schema 校验
- Schema Contract 检查
- 配置检查
- H5/PWA 检查
- mobile 静态检查
- API Contract 检查
- CI Contract 检查
- Env Contract 检查
- 上传清理 dry run
- AI 评测
- readiness 静态检查

本地运行环境:

- 已新增 `cd server && npm run runtime:check`, 用于检查 Node、npm、Docker、docker compose、psql、`DATABASE_URL`、`server/.env` 和根目录 `compose.yaml`。
- 已新增 `cd server && npm run verify:db`, 用于统一执行数据库迁移/种子、`db:check`、清理任务、提醒 dry run 和完整 API smoke。
- 已新增根目录 `api-contract.json` 和 `cd server && npm run api:contract`, 用于约束前后端分离接口、鉴权方式和后端真实路由一致性。
- 已新增 `cd server && npm run schema:contract`, 用于约束核心数据库表、字段、枚举、索引和用户数据级联关系。
- 已新增 `cd server && npm run ci:contract`, 用于约束 GitHub Actions 静态验证和数据库/API smoke workflow 不被误删或降级。
- 已新增 `cd server && npm run env:contract`, 用于约束环境变量模板、安全默认值和部署检查覆盖范围。
- 当前机器尚未安装 `docker` 和 `psql`, 因此本地 `verify:db` 还不能执行; GitHub Actions 的 `Server API Smoke` 会在 CI PostgreSQL service 中执行同一条数据库/API 验收命令。

当前静态检查仍有预期提醒:

- `ADMIN_TOKEN` 需要在内测环境设置为非默认值。
- `LLM_API_KEY` 需要在真实模型评测前配置; 配置后先运行 `cd server && npm run ai:check`, 再运行 `npm run eval:ai -- --output evals/reports/latest.json`, 全部通过后设置 `LLM_READY=true`。
- `INTERNAL_TEST_INVITE_CODE` 需要在外部内测前配置。

## 未完成

### 必须完成后才能外部内测

- 安装 Docker 或提供可连接 PostgreSQL, 执行 `cd server && npm run runtime:check && npm run verify:db`。
- 用 Expo 真机/模拟器跑起 mobile App。
- 真机验证相机、相册、通知权限、Expo push token 获取。
- 配置真实 LLM, 执行 `cd server && npm run ai:check`, 再重新运行 `npm run eval:ai -- --output evals/reports/latest.json`, 人工抽检通过后设置 `LLM_READY=true`。
- 配置真实 OCR 或明确 mock OCR 只用于内部演示。
- 设置内测环境变量: `ADMIN_TOKEN`, `INTERNAL_TEST_INVITE_CODE`, `AUTH_OTP_SECRET`, `CORS_ALLOWED_ORIGINS` 等。
- 用 `/admin/ops-health` 或 `npm run ops:check -- --days 7` 做扩量闸口。

### 必须完成后才能正式上架

- 真实短信/微信验证码发送通道。
- 真实对象存储或预签名上传网关。
- 真实推送网关: Expo/APNS/FCM 或厂商推送。
- iOS IAP / Android 渠道支付, 替换当前 dev/http checkout。
- 商店截图、EAS/Apple/Android 构建账号和审核账号。
- TestFlight / Android 内测包。
- 真机端到端验收: 登录、拍照、OCR、答疑、错题、复习、推送、订阅、注销。
- 法务/合规审核用户协议、隐私政策、未成年人说明。

## 下一步建议

当前最应该做的不是继续堆页面, 而是把 mobile 从“源码骨架”推进到“真机可运行”。

推荐顺序:

1. `cd mobile && npm install`
2. `cd mobile && npm run start:local`
3. 若要连接本机后端, 运行 `cd mobile && npm run api:local`, 在登录页选择模拟器快捷项或填入真机局域网地址。
4. 用 Expo Go 扫码或模拟器验证 App 能启动。
5. 先点击“进入体验演示”, 检查首页、提问、错题、复习、报告、知识图谱是否顺畅。
6. 启动后端和 PostgreSQL, 跑通 OTP 登录、文字提问、拍照识题、错题、复习、报告。
7. 修真机发现的问题。

## 决策规则

- 若 mobile 真机无法稳定跑通登录、提问、错题、复习, 暂停新增功能。
- 若真实 AI 评测不达标, 暂停扩量, 优先修 Prompt 和评测集。
- 若 `/admin/ops-health` 返回 `pause_expansion`, 不进入下一批内测邀请。
- 若支付、推送、OCR 未接真实服务商, 只能算内部演示, 不能算可上线 App。
